import 'package:flutter/material.dart';
import 'package:praktis4/caluhome.dart';

class Calucater extends StatelessWidget {
  const Calucater({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: CaluHome(),
    );
  }
}
